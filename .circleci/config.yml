version: 2
jobs:
  build:
    docker:
      - image: lockblox/prototool
    steps:
      # install etc
      - run: apk add --no-cache git clang bash

      - checkout

      - run: prototool compile
      - run: ./protofmt-diff
